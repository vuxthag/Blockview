{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "▶ Chạy BlockEdu Server (Node.js)",
            "skipFiles": ["<node_internals>/**"],
            "program": "${workspaceFolder}/server.js",
            "console": "integratedTerminal",
            "preLaunchTask": "open-browser"
        }
    ]
}